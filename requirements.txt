# FastAPI and ASGI server
fastapi
uvicorn[standard]
# HTTP client for API calls
aiohttp==3.9.1
# Caching
cachetools==5.3.2
# Environment variables (optional but recommended)
python-dotenv
# Logging (optional - Python has built-in logging)
# But if you want structured logging:
# structlog==23.2.0
# Type hints support (Python 3.8+)
typing-extensions
# JSON handling (usually built-in but explicit version)
# ujson==5.8.0  # Optional: faster JSON parsing
google-cloud-bigquery
google-oauth
pyyaml
pydantic
pandas
pyarrow
db-dtypes
google-cloud-bigquery-storage